#ifndef PRIME_NUMS_HPP
#define PRIME_NUMS_HPP

// Function for checking if the number is prime
bool is_prime(int num);

// Function for finding the n-th prime number
long long int calculate_prime(int n);

#endif